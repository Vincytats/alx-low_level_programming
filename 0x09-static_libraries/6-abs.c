#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _abs - computes absolute value of an integer
 * @n: the number to check
 * Return: absolute value of int
 */
int _abs(int n)
{
return abs(n);
}
