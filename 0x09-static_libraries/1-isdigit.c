#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - checks for a digit (0 - 9)
 * @c: number to be checked
 * Return: 1 if c is a digit, else 0
 */
int _isdigit(int c)
{
return isdigit(c);
}
